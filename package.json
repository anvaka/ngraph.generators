{
  "name": "ngraph.generators",
  "version": "20.0.0",
  "description": "Graph generators library",
  "main": "index.js",
  "scripts": {
    "test": "tap --branches=90 --lines=90 --statements=90 --functions=90 test/*.js",
    "build": "browserify index.js -s generators -o dist/ngraph.generators.js && uglifyjs dist/ngraph.generators.js -o dist/ngraph.generators.min.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/anvaka/ngraph.generators.git"
  },
  "keywords": [
    "graph",
    "generator",
    "random",
    "ngraph",
    "ngraphjs"
  ],
  "author": "Andrei Kashcha",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/anvaka/ngraph.generators/issues"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "tap": "^16.0.1",
    "uglify-js": "^3.14.5"
  },
  "dependencies": {
    "ngraph.graph": "^20.0.0",
    "ngraph.random": "^1.0.0"
  }
}
